<Page x:Class="NassefStore.Views.Products.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Background="#F5F5F5" FlowDirection="RightToLeft">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Padding="12" Margin="0,0,0,12" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="+ إضافة منتج"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="AddProduct_Click" Height="38" Margin="0,0,8,0"/>
                <ToggleButton Grid.Column="1" x:Name="LowStockFilter"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Content="النواقص فقط" Margin="0,0,8,0"
                              Checked="LowStock_Changed" Unchecked="LowStock_Changed"/>
                <ComboBox Grid.Column="2" x:Name="CategoryFilter"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="الفئة"
                          Width="140" Margin="0,0,8,0"
                          SelectionChanged="Category_Changed"
                          DisplayMemberPath="Name"/>
                <TextBox Grid.Column="3" x:Name="SearchBox"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="🔍  ابحث عن منتج..."
                         TextChanged="Search_Changed" Height="38"/>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" Padding="0" UniformCornerRadius="8">
            <DataGrid x:Name="ProductsGrid"
                      AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      SelectionChanged="ProductsGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="45" IsReadOnly="True"/>
                    <DataGridTextColumn Header="اسم المنتج" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="الفئة" Binding="{Binding Category.Name}" Width="110" IsReadOnly="True"/>
                    <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="سعر الشراء" Width="105" IsReadOnly="True">
                        <DataGridTextColumn.Binding><Binding Path="CostPrice" StringFormat="{}{0:N2} ج"/></DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="سعر البيع" Width="105" IsReadOnly="True">
                        <DataGridTextColumn.Binding><Binding Path="SellPrice" StringFormat="{}{0:N2} ج"/></DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="المخزون" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#E8F5E9"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StockQuantity}" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="الحد الأدنى" Binding="{Binding MinStockLevel}" Width="85" IsReadOnly="True"/>
                    <DataGridTextColumn Header="ضمان" Width="70" IsReadOnly="True">
                        <DataGridTextColumn.Binding><Binding Path="WarrantyMonths" StringFormat="{}{0} ش"/></DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="إجراءات" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Width="32" Height="32" Click="EditProduct_Click" Tag="{Binding}">
                                        <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Width="32" Height="32" Click="DeleteProduct_Click" Tag="{Binding}">
                                        <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" Foreground="#C62828"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</Page>
